#pragma once

namespace Visuals {
	/*using ID = unsigned int;
	
	struct Visual {
		ID modelID;
		ID colorID;
	};

	ID addModel(std::string name, ModelData data);
	ID addColor(std::string name, ColorData color);
	ID findModel(std::string name);
	ID findColor(std::string name);
	*/
}